version: '3'
services:
  api:
    image: docker.ngs.vbcf.ac.at/hinkskalle-dev:v0.2.0-1-g8c00abd
    volumes:
      - '.:/srv/hinkskalle/src'
    command: './script/start.sh'
    environment:
      PORT: 5000
      MONGODB_HOST: 'mongodb://mongodb/hinktest'
      MONGODB_USERNAME: 'hinkebein'
      MONGODB_PASSWORD: "${HINKSKALLE_MONGODB_PASSWORD}"
      FSK_URL: 'http://172.28.128.1:6600/fsk3test'
    links:
      - mongodb
    ports:
      - '7660:5000'
  mongodb:
    image: mongo:4-bionic
    volumes:
      - './data/db:/data/db'
      - './data/configdb:/data/configdb'
